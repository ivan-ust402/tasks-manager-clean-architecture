# API Clients (HTTP клиенты) - "Послы в другие страны" 

```
  src/infrastructure/api/
  ├── http/
  │   ├── axios.client.ts          // HTTP клиент
  │   └── interceptors/            // Перехватчики запросов
  ├── PaymentApi.client.ts         // Клиент платежной системы
  └── NotificationApi.client.ts    // Клиент сервиса уведомлений
```